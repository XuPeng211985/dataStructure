package zijietiaodong;

/**
 * Created by hpx on 2018/4/14.
 */
public class Main {
    public static void main(String[] args) {
        int[] arr = {1,2,3};
        int maxT = arr[arr.length-1] - arr[arr.length-2];
        int minT = arr[2] - arr[1];
        for (int i = 0; i < arr.length; i++) {
            if(arr[i] + maxT <= arr[arr.length-1]){

            }
        }
    }
}
